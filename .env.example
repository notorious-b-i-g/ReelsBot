# Скопируйте в .env и заполните. .env в git не попадает.
OPENAI_API_KEY=
TELEGRAM_BOT_TOKEN=

# Прокси для доступа к OpenAI (опционально). Если не задано — запросы идут напрямую.
PROXY_URL=http://your-proxy-host:8000
PROXY_USER=
PROXY_PASS=
